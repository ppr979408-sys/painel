# Database Configuration
USE_MYSQL=false

# MySQL Connection (only needed when USE_MYSQL=true)
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=menu
MYSQL_PORT=3306

# To use real MySQL database instead of test data:
# 1. Set USE_MYSQL=true
# 2. Configure the MySQL connection parameters above
# 3. Make sure the database 'menu' exists and contains the proper tables
# 4. Restart the application

# PostgreSQL (for Drizzle ORM - provided by Replit)
DATABASE_URL=your_postgresql_url_here